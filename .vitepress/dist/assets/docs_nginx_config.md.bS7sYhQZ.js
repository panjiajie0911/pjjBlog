import{_ as i,c as s,o as r,ah as t}from"./chunks/framework.BjT6hq4z.js";const d=JSON.parse('{"title":"配置","description":"","frontmatter":{},"headers":[],"relativePath":"docs/nginx/config.md","filePath":"docs/nginx/config.md"}'),l={name:"docs/nginx/config.md"};function a(n,e,o,p,h,g){return r(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to “配置”">​</a></h1><h2 id="详细讲解-nginx-conf-文件" tabindex="-1">详细讲解 nginx.conf 文件 <a class="header-anchor" href="#详细讲解-nginx-conf-文件" aria-label="Permalink to “详细讲解 nginx.conf 文件”">​</a></h2><h3 id="最顶层" tabindex="-1">最顶层 <a class="header-anchor" href="#最顶层" aria-label="Permalink to “最顶层”">​</a></h3><p>nginx.conf 本身就是最顶层，被称为<strong><em>全局模块</em></strong>，是一个通用配置，旗下分为另外几个模块</p><ul><li>events 模块</li><li>http 模块</li><li>stream 模块</li></ul><p>根据环境的不同，在<strong><em>nginx.conf</em></strong>的同级下，可以设置 env 文件夹，区分不同的环境的配置</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">├──</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> env</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">│</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   ├──</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev.conf</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">│</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   ├──</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test.conf</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">│</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   └──</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> prod.conf</span></span></code></pre></div><p>通过 include 语法引入不同环境的配置文件</p><div class="language-nginx"><button title="Copy Code" class="copy"></button><span class="lang">nginx</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">include </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">env/dev.conf;</span></span></code></pre></div><h3 id="全局模块" tabindex="-1">全局模块 <a class="header-anchor" href="#全局模块" aria-label="Permalink to “全局模块”">​</a></h3><p><strong><em>全局模块</em></strong>的核心配置是这样的：</p><ul><li><p>worker_processes：表示工作进程数，一般是和 CPU 核心数的数量相同的。 nginx 是“单 work 进程 + 多线程 event 驱动”的模式，所以一个 worker 进程只占用一个 CPU。 如果 CPU === worker 进程数，那么会有以下好处：</p></li><li><p>CPU 利用率 100% 。</p></li><li><p>没有进程切换的开销，n 个进程同时处理，处理速度是 1 个 worker 的 N 倍。</p></li><li><p>error_log :指定错误日志的存放路径 日志分级别，默认是 error 级别，级别决定了“什么内容可以写入日志”。可以是 debug | info | notice | warn | error | crit</p></li><li><p>pid: 指定进程 pid 的存放文件的路径, 每一个 nginx 的 master process（主进程）都有一个唯一的 id,即 pid,这里指定的是这个 pid 的文件的存放的位置。</p></li><li><p>worker*rlimit_nofile：所有 worker 进程能打开的最大文件描述符的数量。nginx 会给**_worker process***规定最多能做多少事情，包括***网络连接数***，_**读文件***等等,每一件事情都是一个文件描述符。</p></li></ul><p>nginx 因为是一个高并发的服务器，所以需要限制<strong><em>worker process</em></strong>能做的事情，能避免因为事情太多导致并发太多处理不过来。</p><h3 id="event-模块" tabindex="-1">event 模块 <a class="header-anchor" href="#event-模块" aria-label="Permalink to “event 模块”">​</a></h3><p>event 模块是拿来规定<strong><em>事件驱动</em></strong>的工作方式的，nginx 的<strong><em>事件驱动</em></strong>的模式规定了它只响应有需求的请求，所以需要规定当请求过来的时候，<strong><em>worker process</em></strong>该用什么样的方式去处理这个请求。</p><ul><li>worker*connections: 单个<strong><em>worker process</em></strong>最大并发链接数，即一个**<em>worker process</em>**能同时处理的链接的数。</li><li>accept*mutex: 对多个<strong><em>worker process</em></strong>接收连接进行序列化，防止多个进程对连接的争抢。意思是一个请求来的时候，多个<strong><em>worker process</em></strong>可能存在争抢。开启这个配置后， 那么 ***worker process*** 就会按照顺序来接收这个请求。</li><li>multi_accept: 是否允许同时接收多个网络连接</li><li>use: 指定事件模型，epoll | select | poll。epoll 模型是<strong><em>高并发</em></strong>的核心。当有<strong><em>事件驱动</em></strong>时候，<strong><em>worker process</em></strong>立即去响应它。select | poll 模式是每隔一段时间去访问<strong><em>worker process</em></strong>，效率低下。</li></ul><h3 id="stream-模块" tabindex="-1">stream 模块 <a class="header-anchor" href="#stream-模块" aria-label="Permalink to “stream 模块”">​</a></h3><p><strong><em>stream 模块</em></strong>用来处理原始的流数据，它处理所有<strong><em>非 http 请求</em></strong>。它在<strong><em>TCP/UDP 协议层</em></strong>（即网络的第 4 层），只认“IP + 端口”，不解析请求头，请求内容。主要配置如下：</p><ul><li><p>access_log: 权限日志配置</p></li><li><p>error_log: 错误日志配置</p></li><li><p>upstream + 服务器名称 {</p><p>后端服务器组的配置，所有和 TCP/UDP 转发相关的配置都在这里。多个服务器以组合的形式出现在这里。</p><ul><li>server + 服务器地址 + 端口：默认权重为 1。</li><li>server+ 服务器地址 + 端口 + weight:手动设置权重。</li><li>server+ 服务器地址 + 端口 + backup:备用节点。</li><li>least_conn :负载均衡策略选择**_按需分配请求_**。 负载均衡是指多个请求配到服务器的模式，有下面几个模式 <ul><li>轮询模式（默认）：按顺序循环分配。</li><li>least_conn：假如说有 n 台服务器，请求会把更多的请求优先分配给当前活跃连接最少的。</li><li>ip_hash：客户端 IP 哈希，固定客户端对应后端服务器。</li><li>weighted 轮询：只轮询<strong><em>指定权重</em></strong>的服务器。</li></ul></li></ul></li></ul><p>}</p><h3 id="http-模块" tabindex="-1">http 模块 <a class="header-anchor" href="#http-模块" aria-label="Permalink to “http 模块”">​</a></h3><p><strong>_ http 模块_</strong>处理 HTTP/HTTPS 协议请求的核心模块。 {</p><ul><li><p>log_format：自定义访问日志的格式和内容 main &#39;$remote_addr - $remote_user [$time_local] &quot;$request&quot; &#39; &#39;$status $body_bytes_sent &quot;$http_referer&quot; &#39; &#39;&quot;$http_user_agent&quot; &quot;$http_x_forwarded_for&quot;&#39;;</p></li><li><p>access_log：访问日志的地址</p></li><li><p>error_log: 错误日志的指定路径和日志级别</p></li><li><p>keepalive_timeout:http 链接超市时间&#39;</p></li><li><p>gzip: 是否启用压缩。on 表示开启，off 表示关闭。</p></li><li><p>gzip_types: 指定需要压缩的文件类型。文件类型可以是： text/html text/css application/json application/javascript 等等。</p></li><li><p>client_max_body_size：C 端传输过来的文件的最大大小，默认是 1m</p></li><li><p>tcp_nopush：一次性发送数据，减少网络传输次数。</p><p>同时，http 模块也可以配置<strong><em>stream 模块</em></strong>，内容和上述一致。</p><p>}</p></li></ul><h3 id="server-模块" tabindex="-1">server 模块 <a class="header-anchor" href="#server-模块" aria-label="Permalink to “server 模块”">​</a></h3><p><strong>_ server 模块_</strong>可以在 http 模块和 stream 模块都配置，用于设置服务相关的配置。一个<strong>_ server 模块_</strong>代表的就是一个虚拟机。 {</p><ul><li><p>listen: 指定 Nginx 监听的端口（可指定 IP）；比如 192.168.1.1:80;或者 80（所有 ip 的 80 端口）</p></li><li><p>server_name：绑定的任意个数的域名。</p></li><li><p>root: 指定当前虚拟主机的静态网站根目录。</p></li><li><p>index: 指定首页文件，当访问这个 server_name 的时候，自动查找该文件。</p></li><li><p>ssl_certificate： 如果是 https 协议，则指定 ssl 证书的路径。</p></li><li><p>ssl_certificate_key: 与上同理，则指定 SSL 私钥文件路径。</p></li><li><p>error_page：网页错误时候的 html 文件</p></li><li><p>location ：{</p><ul><li>=符号是<strong><em>精确匹配</em></strong>，仅匹配完全一致的 URL 路径，比如=/index.html</li><li>^~符号是<strong><em>前缀匹配</em></strong>，匹配以指定路径开头的 URL，且不进行正则匹配</li><li>~符号是<strong><em>正则匹配</em></strong>，匹配以指定路径开头的 URL，进行正则匹配</li><li>/符号是<strong><em>普通前缀匹配</em></strong>，匹配以指定路径开头的 URL。</li></ul><p>参数配置如下：</p><ul><li>root:表示当 location 被匹配上之后，在这个路径下查找资源</li><li>allow/deny：指定的 ip 才能访问或者禁止访问。</li><li>index:表示当 location 被匹配上之后，则访问这个 index.html 文件</li><li>expires ：表示是否关闭缓存资源，以及缓存的天数 proxy_pass ：反向代理到指定的服务器 <h1 id="传递真实客户端-ip-和-host" tabindex="-1">传递真实客户端 IP 和 Host <a class="header-anchor" href="#传递真实客户端-ip-和-host" aria-label="Permalink to “传递真实客户端 IP 和 Host”">​</a></h1> proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; <h1 id="连接超时配置" tabindex="-1">连接超时配置 <a class="header-anchor" href="#连接超时配置" aria-label="Permalink to “连接超时配置”">​</a></h1> proxy_connect_timeout 10s; proxy_read_timeout 30s; } }</li></ul></li></ul>`,26)])])}const m=i(l,[["render",a]]);export{d as __pageData,m as default};
